# 개발 로드맵

기준일: 2026-09-16. **기존 PC 범위 이후, D415 주행 장애물·지도 변경·Nav2 브릿지와 전체 작업의 자세/관측 전환까지 보완했다.** 다음 단계는 실제 장비를 연결해 소파의 리모컨을 침대의 지정 영역에 배달하는 Classical 시스템을 완성하는 것이다. 완료한 PC 작업을 다시 개발 일정으로 잡지 않는다.

## 1. 최종 목표와 완료 판정

| 목표 | 완료로 판단할 결과 | 필요한 단계 |
|---|---|---|
| G1. Classical 자율 배달 | 등록된 실내에서 요청 → 소파 접근 → 필요 시 다른 시점 탐색 → 리모컨 집기·유지 → 침대 이동 → 지정 면에 놓기·확인. 반복 평가와 실패/취소까지 통과 | H0–H6 → E1 |
| G2. 언어·영상 요청 | “소파에서 리모컨 찾아 침대에 놔줘”를 지원하는 물체·장소·작업으로 해석하고 G1 실행기에 전달. 모호함·미지원·오래된 관측을 실행으로 바꾸지 않음 | G1 + U1(VLM) |
| G3. 휴대폰 지도 이동 | 휴대폰의 입체 지도에서 주행 가능한 바닥 지점을 선택해 실제 이동·진행 조회·취소. 로봇 배달과 제어권 충돌 없음 | H4 + A1; 전체 배포 전 E1 운영 검사 |
| G4. VLA 비교 | 리더암 시연으로 정지 플랫폼의 팔·그리퍼 정책을 학습하고, 동일 작업·평가 배치에서 Classical과 성공률·시간·개입률 비교 | G1 기준선 고정 → V1 |

G1이 첫 실물 시연 목표다. G2·G3는 요청한 확장 기능이고, G4는 기존 프로젝트의 후속 비교 목표다. **G1 완료만으로 VLM·휴대폰·VLA까지 완료했다고 하지 않는다.** VLA 비교 완료는 Classical보다 성능이 높다는 보장을 뜻하지 않는다.

### 이 로드맵으로 목표에 도달할 수 있는가

위 목표에 필요한 기구·제어·관측·인식·집기·배달·사용자 입력·평가 경로를 모두 포함한다. 다만 현재 결과는 PC 검증이므로 실물 성공을 보장할 수 없다. 다음 불확실성을 H0/H3/H5에서 먼저 해소하고, 실패하면 해당 장착/기구/관측 방법을 수정한 뒤 다음 단계로 간다.

- **물리적 가능성:** 실제 소파·침대 높이, 베이스 접근 여유, 리프트 범위, 팔 도달 범위, 그리퍼의 리모컨 두께/무게 적합성. 계획 성공만으로 집기 가능성을 판정하지 않는다.
- **볼 수 있는가:** 정면에서 안 보이면 등록된 다른 베이스 위치·리프트 높이에서 재탐색한다. 모든 허용 시점에서 완전히 가려지거나 쿠션 속에 묻힌 물체는 미확인/접근 불가로 종료한다. 쿠션 이동이나 가구 내부 뒤지기는 현재 목표에 포함하지 않는다. 이를 필수로 요구하면 별도 조작 능력이 필요하다.
- **잡고 유지할 수 있는가:** 그리퍼 위치/노력과 시각 등 실제 확보 가능한 증거로 집힘·운반 유지·해제를 구분한다. 현재 센서로 판별이 안 되면 관측 위치/절차 또는 센서·기구를 보완한다. 명령 성공을 집힘 증거로 대체하지 않는다.
- **놓을 수 있는가:** 침대의 지정 면이 실제로 접근 가능하고 물체를 지지해야 한다. 깊이 평면 검사만으로 매트리스의 강성·변형·물체 안정을 보장하지 않는다.

초기 운용 범위는 알려진 평탄한 실내, 등록 장소, 지정 리모컨 같은 가벼운 강체다. 임의의 집·모든 물건·빨래/변형체·계단·전신 동시 학습 제어는 완료 조건에 넣지 않는다. 양팔 플랫폼이지만 대표 작업에 불필요한 양팔 전달을 추가하지 않는다.

## 2. 완료한 PC 범위

| 영역 | 완료한 구현·검증 | 실물에서 남은 일 |
|---|---|---|
| F1–F3 펌웨어·ROS 계약 | 팔 12축 v2 유지, 모바일 별도 envelope, 공유 UART 단일 소유자, 주기 DMA/RX, idle 팔 관측, zero→hold·AQ/AT·AE/AF·명시적 복구 | 실제 전기/버스 타이밍, 제동·하중 유지, 관측/interlock 연결 |
| B1·L1 베이스/리프트 | 옴니 기구학·실측 속도 기반 odometry, lift 높이/원점/경계/정지 코어, 설정 생성·hash·모드 readback | 방향/단위/치수/원점·제동·지지 실측 |
| R1 모델/관절 | 조립체 모델, 관절 설정 생성/일치 검사, 양팔 좌표·범위 검사 | 새 장착의 전체 기계 범위·TF·충돌 형상·jaw 보정 |
| S2 관측·인식 경계 | RGB-D/TF·동결 영상, ONNX 입력/출력 계약, EvidenceProducer와 typed ROS 생산/소비, 원 관측 시각 보존 | 모델 자산/실영상 평가, driver/topic/시각/TF 연결, 실제 지지/물체 관측 |
| I1–I2 집기·배달 | 전체 15단계, 내부 집기/놓기 11단계, 공유 그리퍼·단계별 관측 binding, scene 부착/분리 readback, depth 표면 검사 | 실제 접근/후퇴·scene 공급자·충돌 검증·집힘/배달 확인 |
| N1·P1 알고리즘 연결 | 실제 Nav2/AMCL→C 바퀴 모형 검사, 실제 MoveIt IK/계획/Ruckig/충돌·부착 검사, ROS 모의 전체 fetch | 센서·MCU·Nav2·MoveIt 동시 실기 통합 |
| A1 앱 기반 | 인증 HTTP·짧은 permit·SQLite 중복/재시작, 지도 지점 선택·입체 점유 지도·이동/취소 | 실물 adapter, 휴대폰 LAN/TLS/페어링, 실측 지도 연결 |
| D1 재현성 | 통합 검증 도구, 소스/설정 hash, 두 STM32 빌드, 장애·취소·가속 반복과 후보 묶음 | 목표 보드 설치·부하/열·장시간 운용·원격 CI |

2026-09-16 최종 PC 후보: **Python 793개, CTest 17개, STM32 두 빌드, ROS 4개 패키지**와 실제 DDS/C·Nav2 costmap·MoveIt·가속 모의 1시간을 포함한 33개 검사. 최신 소스/설정/로그는 `output/runtime-closeout-20260916/report.json`이다. 과거 보고서는 보존하며 현재 소스의 통과 증거로 소급하지 않는다. 실제 Nav2/AMCL 전체 알고리즘 주행은 2026-09-15 기록이다.

관측 높이에서 정렬하기 전 운반 자세 복귀, 새 travel epoch 관측 대기/시간 초과, 발견한 시점의 접근점/높이 유지, 가구 변경 시 등록 폐기/정지, payload 카메라 출처 검사를 완료했다. 전체 15단계에 주행 guard와 모의 wheel feedback을 연결하고 관측 누락·복귀·운반 중 상실·취소를 검사했다. 모의 free 공간은 실물 카메라 시야 증거가 아니다.

이 보고서는 검증 당시 소스 묶음의 기록이다. 이후 문서 변경을 기존 해시에 소급 반영하지 않는다. 실물 명령·플래시·물리적 배달은 수행하지 않았다. 새로운 요구나 발견된 결함이 없으면 PC 작업을 늘리지 않고 이 범위를 마감한다.

## 3. 가장 빠르게 목표에 도달하는 순서

```text
PC 후보 → H0 장비/환경·전신 시야/도달 가능성 확인
                  ├→ H1 바퀴·통신·정지 ────────────→ H4 주행·정차 ──┐
                  ├→ H2 리프트·원점·하중 → H3 팔·TF/관측 → H5 집기 ─┤
                  └→ 실제 영상/모델 평가 ───────────────────→ H5 ─┤
                                         H4 + H5 → H6 전체 배달 → E1 기준선
                                         H4 → A1 휴대폰 이동       ├→ U1 VLM
                                                                 └→ V1 VLA 비교
```

- H0에서 컴퓨팅 환경과 물리적 도달 가능성을 먼저 확인한다. 끝에서 ROS 환경을 바꾸거나 집을 수 없는 자세를 뒤늦게 발견하는 재작업을 줄인다.
- H4 주행과 H5 정지 플랫폼 집기는 독립적으로 검증한다. 같은 실물을 쓰는 구동 시험은 시간대를 나누고, 영상 평가/로그 분석/설정 작업을 병행한다. 한 시험 중 다른 writer가 모터를 제어하지 않는다.
- H5는 쉬운 지정 면 → 실제 소파/침대 순서, H6는 짧은 이동 → 방 간 배달 순서다. 전체 자율 배달에서 처음으로 각 부품의 고장을 찾지 않는다.
- 기준 주행은 LiDAR 한 대로 만든다. 두 번째는 가림/위치 추정 개선 효과를 확인해 추가한다. 기준 배달은 필요한 한 팔로 시작하고 양팔 기능은 필요할 때만 사용한다.
- VLM/네이티브 앱/정교한 3D mesh/VLA 학습은 첫 배달의 선행 조건이 아니다. 다만 실제 영상·실행 로그는 H3부터 저장해 후속 인식 평가와 시연 수집의 재작업을 줄인다.
- 속도는 H4/H5부터 구간별로 측정하고 H6에서 큰 지연부터 줄인다. 모든 튜닝을 E1 뒤로 미루거나 전체 감속으로 떨림을 덮지 않는다.
- 기존 ‘도착 전 14일 계획’은 완료 이력으로 대체했다. 남은 일정은 H0에서 확인한 환경/기구 문제를 기준으로 잡고, 각 통과 기준에 미달하면 해당 단계만 보완한다.

## 작업별 구현 범위와 검증

아래는 **남은 작업**이다. 기존 F1–F3의 프로토콜·소유권·정지 구현을 다시 만드는 계획이 아니다. 각 단계에서 실제 driver binding, launch/config, 관측 제공자, 배포 승격 코드는 필요에 따라 추가해야 한다. ‘실측 숫자만 넣으면 끝’이라는 상태는 아니다.

### H0. 장비 확인·실행 환경·물리적 가능성

**선행:** PC 후보 검증 완료. **후속:** H1–H3과 실영상 수집의 공통 기반.

- ALOHA Mini 1 실부품·전원 분배·전압/전류·배선을 확인한다. 왼쪽 공유 버스는 팔 1–6 + wheel 8/9/10 + lift 11, 오른쪽은 별도 UART의 팔 1–6이다. 실제 모터 모델·ID·baud·mode를 readback한다. Waveshare의 두 커넥터를 독립 통신 채널로 취급하지 않는다.
- 보유 Jetson/Pi 중 한 대의 실제 OS/ROS/SDK·GPU/USB·메모리·저장/전원/냉각을 확인하고 실행 환경을 고정한다. 기존 우선 후보는 Jetson이며 확정은 실제 센서/추론 동시 구동 결과로 한다. PC Jazzy 환경과 동일하다고 가정하지 않는다. 배포판/컨테이너 변경이 필요하면 먼저 프로토콜·ROS·MoveIt 회귀를 확인한다.
- D415 변경 상태를 확인하고 실제 카메라 기준으로 설정한다. LDS-01은 2D 위치 추정/주행, 리프트 D415는 정지 집기 + 주행 상부 장애물 관측, 전/후 RGB는 의미 탐색을 맡는다. 보유하지 않은 IMU·리더암·손목 카메라를 필수 장비처럼 설정하지 않는다.
- D415가 주행 공간을 보는 자세에서 그리퍼/물체를 계속 볼 수 있는 카메라와 관측 지연을 먼저 확인한다. 불가능하면 운반 자세·카메라 배치/센서를 보완한다. 영상이 가려졌다는 이유로 load_retained를 유지하지 않는다.
- 실제 소파·침대·통로·리모컨 치수/무게와 카메라 장착 후보를 측정해 접근/높이/시야/그리퍼 공간을 확인한다. 간섭이나 도달 불가면 마운트·접근 방향·작업 위치를 먼저 수정한다. 원래 목표를 줄여서 통과 처리하지 않는다.
- 모델/원본 의존성 리비전, 코드·펌웨어·설정 hash와 원복 가능한 배포본을 보관한다. 물리 실행용 검증 절차와 축별 시험 진입점을 마련하되 자동 ARM/homing/이전 작업 재개는 금지한다.

**통과:** 장치 목록·실행 환경 고정, 중복 ID/전원 문제 없음, 기구 검토상 대표 물체의 집기/놓기 후보가 존재함. H2/H3/H5에서 실제로 성립하는지 확정한다. 환경 미확정 상태로 모든 노드를 한꺼번에 이식하지 않는다.

### H1. 베이스·공유 버스·기본 정지

- 바퀴를 지지한 상태에서 개별 방향/속도 단위/피드백을 확인하고 순수 전후·횡이동·회전을 시험한다. 명령 echo 대신 실제 wheel feedback으로 odometry를 만든다.
- 팔 tick·wheel/lift writer·READ가 함께 있을 때 wire time·turnaround·timeout·최대 관측 나이를 실측해 버스 예산을 설정한다. STM32만 UART를 소유하며 별도 LeRobot Host를 동시에 붙이지 않는다.
- 통신 단절·누락/늦은 응답·host 종료·재부팅·오래된 명령에서 zero 전송과 실제 바퀴 정지를 구분한다. 재연결/재시작 시 자동 이동하지 않아야 한다.
- 추후 H2/H3의 하중 유지 확인을 합쳐 전체 정지를 검증한다. 바퀴 정지만 확인하고 전체 로봇 정지가 완료됐다고 하지 않는다.

**통과:** 방향/단위/odometry 기본 검증, 시간 예산 내 피드백, 장애 시 정지·재명령 차단. 지상 주행용 속도/제동 거리 후보를 기록한다.

### H2. 리프트·원점·독립 지지 증거

- 기구를 지지한 상태에서 명시적 torque-off 유지보수·mode provisioning/readback → disabled 새 boot → 신선한 관측 → 별도 homing 순서로 시험한다. 일반 부팅에 EEPROM 변경·자동 하강을 넣지 않는다.
- 높이/회전 변환, encoder wrap/점프, 상하 끝점·기구 여유, homing 반복성/timeout/오탐, 속도별 제동 거리와 정지 후 처짐을 측정한다. 현재 접근 감속 코드를 실측 제동 모델로 보완한다.
- 무부하 다음 실제 팔·물체 하중에서 유지/정지를 확인한다. 속도 0·TX 완료·torque off를 하중 지지로 간주하지 않는다. 통신/전원 상실 대응이 부족하면 지지 기구/전원 구성을 보완한다.
- 실제 interlock/hold 증거를 기존 lift·stop 입력에 연결한다. 시험용 지지 상태와 자율 운용에서 가능한 지지를 구분한다.

**통과:** 원점/높이/경계·제동 허용치, 지정 하중 유지, 오류 시 원점 폐기·재구동 차단. 실측 기록으로 lift profile 생성.

### H3. 팔·카메라·실측 관측과 충돌 장면

- 좌우 관절 영점/방향/기계 끝점·배선 여유를 저속으로 측정한다. 예전 작업의 **관절 범위**를 새 기구에서 확인된 범위로 갱신하고 raw/model/ROS/펌웨어 설정을 함께 생성·검사한다. 고정 Z 제한을 푸는 작업으로 대체하지 않는다. 팔 베이스 아래 집기도 실제 자기/환경 충돌을 통과해야 한다.
- 리프트 높이별 base/lift/팔/카메라/그리퍼 TF와 collision geometry, 실제 joint_states를 연결한다. jaw 각도→간격·open/close 방향을 보정한다. 기존 고정 작업대 변환을 복사하지 않는다.
- arm/base/localization/lift/scene driver 출력을 EvidenceProducer에 연결하고 원 취득 시각·boot/profile·sequence를 보존한다. ROS 생산/소비와 AE/AF 입력을 실측 상태로 검증한다. 장치 누락을 임의 true로 채우지 않는다.
- 실제 scene의 단일 writer·revision, 물체별 attached/detached, fresh stage pose, 독립 reach/collision 검사 제공자를 구현·연결한다. ResidentRouteBuilder의 validator는 **전체 경로**를 현재 장면에서 검사해야 한다. 이 연결은 아직 끝난 소프트웨어로 간주하지 않는다.
- 카메라 정렬·깊이 단위·노출 TF·팔 가림을 확인하고 실제 소파/침대 RGB-D를 저장한다. ONNX 모델 파일/hash·클래스를 선택하고 리모컨 오검출/누락·깊이 실패를 평가한다. 조정 영상과 최종 평가 영상을 분리한다.
- 전체 zero→양팔/lift hold의 후속 관측을 실제로 확인한다. 취소·tracking fault·관측 상실·재부팅 후 제어권을 성급하게 해제하지 않는다.

**통과:** 실측 설정·TF·관측 공급, 저속 범위/자기 충돌·그리퍼 검증, 전체 정지/하중 증거 연결. 카메라에서 본 대표 목표를 실제 팔 좌표와 대조할 수 있음.

### H4. Nav2 주행·재탐색 위치·정차

**선행:** H1, 운반 중 팔/lift가 유지되는 H2/H3의 확인. H5 집기와 구동 시간을 나눠 진행한다.

- 먼저 주행 자세의 D415 커버리지·진동·낮은/높은 장애물·near blind spot·후진/횡이동 사각을 검증한다. base/lift 정지→운반 높이/팔→새 depth epoch/관측→이동 순서를 확인한다. 시야 부족이면 마운트/보완 센서를 먼저 수정하고 속도 최적화를 진행한다.
- 베이스 단독 방향·거리/회전 odometry → LiDAR 지도/위치 추정 → Nav2 왕복 → 반복 정차 순서로 검증한다. 실제 footprint·팽창 여유·운반 자세·제동 거리를 반영한다.
- 소파/침대 접근점과 유한한 재관측 위치·리프트 높이를 등록한다. Nav2와 미세 정렬기는 베이스 명령 소유권을 공유하며 동시에 쓰지 않는다. 도착 결과 다음 실측 정지를 확인한다.
- 책상을 A→B로 옮겨 B 회피, A의 상판 기억 유지/깊이 재관측 삭제, 재위치 추정을 시험한다. 2D LiDAR가 상판을 삭제하지 못해야 한다. 기준 map은 지속 변경 확인 뒤 정지→revision 교체→catalog 재검증→재위치 추정 순서로 갱신하며 매 프레임 저장하지 않는다.
- 좁은 통로·경로 차단·위치 추정 상실·scan/TF 지연·취소를 시험한다. 두 번째 LiDAR는 시각/TF를 맞추고 효과를 비교한 뒤 적용한다.
- 실측 정차 오차를 팔 도달/카메라 시야와 비교하고 미세 정렬을 보정한다. 방향/속도뿐 아니라 횡미끄러짐·실제 바닥 영향을 기록한다.

**통과:** 대표 장소 왕복·정차 허용치와 유한 실패/취소. A1의 실물 이동 검증을 시작할 수 있다.

### H5. 정지 플랫폼에서 실제 집기·놓기

**선행:** H2/H3. 초기 시험에서 베이스를 고정하며 H4 완료를 기다릴 필요는 없다.

- 쉬운 지정 면에서 `PickPlacePort`의 집기 6단계·놓기 5단계를 실제 공유 팔/그리퍼, fresh stage observation, MoveIt 장면에 연결한다. factory 미주입 상태의 접근 계획 전용 경로를 전체 집기로 취급하지 않는다.
- 그리퍼 열기 → 접근 → 닫기/집힘 → scene attach/readback → 하중 유지/후퇴와, 목적지 접근 → 해제 → detach/readback → 후퇴/배달 확인을 각각 검증한다. 물체 크기·link/world pose·touch links를 실측한다.
- 그리퍼 미집기/빈 집기·운반 중 미끄러짐·낙하·미해제·scene 응답 유실·단계별 취소에서 실제 결과를 구분한다. 부족한 관측은 새 센서 또는 재관측 절차로 해결한다.
- 다음으로 실제 소파와 침대에서 깊이/가림·리모컨 두께/방향·쿠션/매트리스 변형·진입/후퇴 여유를 확인한다. SurfaceObserver의 평면 통과만으로 놓기 성공을 보고하지 않는다.
- 운반 자세·하중·베이스/lift 상태에 맞는 허용 동작과 전체 충돌 검증을 확정한다. 팔 품질은 아래 기준으로 함께 조정한다.

**통과:** 실제 대표 물체를 사람의 위치 보정 없이 집고 유지하고 지정 영역에 놓는 동작, 관련 실패/취소 검증. 주행 없이 먼저 성공 경로를 확보한다.

#### 팔 떨림과 시연 시간

PC 구현은 기존 어깨 P64/I0/D64·팔꿈치 P56/I0/D64 유지, 좌우 runtime 필드 일치, task waypoint 5차 시간 곡선, MoveIt Ruckig까지 완료했다. P16 후보는 철회했다. **실물 튜닝은 미완료**다.

- 사용자 증상인 접힌 팔이 일어나는 구간과 다시 접히기 직전을 같은 목표/하중/시작 자세로 재현한다. 실제 register, 목표/실측 궤적, 전류/전압·유격을 기록해 목표 자체의 떨림과 servo/기구 반응을 구분한다.
- 낮은 P의 하중 오차 이력을 고려해 기존 P를 기준으로 원인에 따라 D, 경로·가속/jerk를 한 변수씩 비교한다. 무조건 D를 높이거나 전체 팔을 감속하지 않는다.
- 총 이동+정착 시간, 추종 RMS/최대 오차, 정지 hold 진폭, 하중 유지를 함께 비교한다. `tools/run/analyze_arm_motion.py`를 사용하고, 위치 표본으로 못 보는 진동은 별도 관측한다.
- 허용 오차·하중·시간을 함께 통과한 값만 승격한다. 범위/추종 정지 기준을 느슨하게 만들어 통과시키지 않는다. 프로파일 변경 시 firmware/ROS hash와 회귀를 함께 갱신한다.

### H6. 전체 배달·실물 실행 구성

**선행:** H4 + H5 및 전체 정지 검증.

- 실제 Nav2·MoveIt·센서·MCU·관측/scene 제공자를 동일 runtime에 조립한다. 단일 UART/scene 소유권, 실제 launch/config·시작/종료 순서, 모델/보정 누락 차단을 구현한다.
- 현재 `RobotApplication`의 physical 차단은 그대로 우회하지 않는다. H1–H5의 축별/정지 플랫폼 시험 경로와 구분된 **검증용 전체 실물 실행 경로**를 만들고, 측정 profile·장치 식별·원점·신선한 상태·정지 준비를 확인한 뒤 명시적으로 시작하게 한다. 검증 없이 simulation 플래그만 바꿔 실물을 허용하지 않는다. 일반 사용자 실행 승격은 E1 뒤다.
- 짧은 이동을 포함한 지정 면 배달 → 실제 소파→침대의 15단계로 확장한다. 이동/리프트 변경 후 재관측, 주행 중 운반 자세·하중, 조작 중 base/lift 정지를 계속 감시한다.
- 정면 미발견→다른 시점 발견, 완전 가림, 대상 없음, 접근 불가를 구분한다. 현재 기본 탐색 후보 32개·시점당 촬영 2회(상한 3회)·탐색 120초·전체 300초는 **PC 후보값**이다. 실제 측정으로 재시도/시간 예산을 정하고, 시연 목표 시간과 timeout을 구분한다.
- 모든 단계의 취소·통신/센서/위치 추정 상실·재부팅·물체 상실을 시험한다. stop 확인 전 소유권 해제/다음 작업 금지, 새 disabled boot·재원점·상태 확인 후 명시적 복구, 이전 작업 자동 재개 금지를 유지한다.

**통과:** 실제 배달 완료의 독립 증거와 구간별 시간 기록. 실패/취소/복구까지 포함한 E1 반복 평가를 수행할 수 있음.

### E1. 반복 평가·시간 최적화·운용 승격

평가 전에 아래 표의 후보를 측정 가능한 수치로 고정한다. 시험 결과를 본 뒤 실패 사례를 빼거나 기준을 완화해 통과시키지 않는다. 기본 제안은 **지원 조건의 배달 20회 중 18회 이상 무개입 성공**이며, 아직 합의된 실기 성능이나 달성 결과는 아니다.

| 평가 축 | 고정할 기준·기록 | 완료 판정 |
|---|---|---|
| 배달 결과 | 리모컨 위치/방향·가구 높이·조명·접근 조건을 나눈 고정 배치, 전체 시도 수·실패 원인 | 지정 영역 놓기·해제·후퇴 확인, 무개입 성공률 기준 통과 |
| 미발견/가림 | 재탐색 후 발견, 실제 부재, 완전 가림, 도달 불가 별도 사례 | 유한 종료, 보지 못한 것을 부재/성공으로 단정하지 않음 |
| 오차/하중 | 정차 x/y/yaw, 리프트 높이, 목표 좌표/집기/놓기 오차, 처짐·물체 유지 | H0–H5에서 정한 실측 운용 허용치 충족 |
| 시연 시간 | 정해진 경로·하중의 목표 `T_demo`, 전체/주행/리프트/관측/계획/실행/정착 p50·p95 | H6 기준 측정 뒤 평가 전에 정한 목표 충족; timeout 이내라는 이유만으로 빠르다고 하지 않음 |
| 장애/취소 | 각 단계 취소, 관측/통신 손실, reboot·낙하·scene 불일치·회복 | 정의한 정지 시간/거리·하중 유지, 의도치 않은 재구동/성공 오보고 0건 |
| 운영/재현 | 목표 보드 동시 부하·열/전원·USB, 여러 작업/재시작, 고정 버전 설치·원복 | 제어/관측 시간 예산 유지, 재시작 뒤 자동 ARM/homing/작업 재개 없음 |

먼저 불필요한 대기·중복 촬영/계획·과도한 정착을 줄이고, 그다음 측정된 추종/정지 여유 안에서 속도·가속/jerk를 조정한다. 총 시간 목표를 못 맞추면 G1의 시연 품질은 미완료로 남긴다. 성공률/안전을 희생해 시간만 맞추지 않는다.

E1을 통과한 코드·펌웨어·모델·보정·지도 revision과 결과를 묶어 G1 기준선을 고정한다. 일반 실물 요청 경로의 승격, 설치/재시작/원복 절차, 원격 CI 확인까지 마친다. 임시 의존성 경로나 현재 MoveIt 종료 우회가 필요한 경우 목표 환경에서 재현하고 배포에 명시한다.

## 4. 요청한 사용자 기능과 후속 비교

### U1. VLM 언어·영상 요청

- 모델은 물체/장소/작업 후보를 기존 MissionCatalog와 정형 요청으로 변환한다. 저수준 모터 명령이나 관절 제한을 직접 출력하지 않는다. 문장 해석·화면 후보 선택과 RGB-D 위치/MoveIt 검증의 책임을 분리한다.
- H3의 실제 영상과 지원 문장으로 모델·메모리·지연을 평가한다. 모델 선정/오프라인 평가는 H5/H6와 병행할 수 있지만, 실제 자동 실행은 G1의 검증된 경로 위에 연결한다.
- 미지원 물건/장소, 복수 리모컨, 모호한 지시, 오래된 영상·추론 timeout을 거절하거나 확인 요청으로 반환한다. 완전 가림을 언어 추정으로 해결했다고 하지 않는다.
- 센서/주행/제어 동시 부하에서 추론이 시간 예산을 깨지 않는지 확인한다. 온보드 모델이 적합하지 않으면 크기/실행 위치를 별도 결정하며 현재 Jetson만으로 가능하다고 가정하지 않는다.

**G2 완료:** 고정한 문장/영상 평가에서 해석 기준 통과, 잘못된 요청의 동작 차단, 언어 요청으로 G1 배달과 취소 성공. VLM을 붙였다는 이유로 VLA 완료로 표시하지 않는다.

### A1. 휴대폰 입체 지도에서 지정 지점 이동

- 기존 브라우저 UI·HTTP gateway를 재사용한다. H4 이후 실측 주행 지도와 표시 지도의 frame/floor/revision을 연결하고, 실제 Nav2/정지 관측 adapter로 검증한다. 현재 simulation 전용 제한을 검증 없이 제거하지 않는다.
- 입체 점유 지도에서도 주행 목표는 바닥의 x/y/yaw다. 클릭 위치·방향을 미리 보여주고, 가구 위 클릭/지도 교체/오래된 permit/경로 불가를 거절한다. 실측 mesh/점군은 표시 품질 확장이며 첫 기능 완료를 막는 조건으로 삼지 않는다.
- 실제 휴대폰에서 LAN/TLS·인증/페어링·토큰 폐기·상태/취소·재연결을 확인한다. 기본 loopback에서 외부 연결로 바꾸는 것은 별도 검증이다. 배달 중 지도 요청·중복 요청·서버 재시작에 기존 소유권/복구 규칙을 적용한다.
- 네이티브 앱 패키징은 별도 선택 사항이다. 첫 완료는 **휴대폰 브라우저에서 실제 지도 선택→이동→취소**이며, 같은 API를 나중에 네이티브 앱에서 사용할 수 있게 유지한다.

**G3 완료:** 지정점 실제 정차 허용치, 경로 차단/위치 추정 상실/재연결/제어권 충돌 검사 통과. H4 뒤 제한된 검증을 시작하고 E1 운영 기준으로 일반 사용을 승격한다. VLM은 필요하지 않다.

### V1. 리더암 시연·VLA 학습·Classical 비교

- G1 기준선을 먼저 고정하고 리더암/카메라/학습 장치의 확보와 대응을 확인한다. 현재 보유 목록만으로 수집/학습 환경이 준비됐다고 가정하지 않는다.
- 영상·언어·관절 상태/행동·시각·성공 결과를 동기화하고, 리더/팔·그리퍼 매핑과 수집 안전 경로를 검증한다. 소수 시연으로 기록/재생 파이프라인을 확인한 뒤 충분한 데이터 수집과 학습을 진행한다.
- 학습·검증·최종 평가의 배치/시연을 분리한다. 초기 정책은 **정지한 base/lift 위의 팔·그리퍼**만 제어하며, 주행·리프트·제한·결과 판정은 공통 실행기에 둔다.
- 기록 입력/모의 실행 → 지지/저속 실물 → 대표 배치로 평가한다. 제어 주기·추론 지연·관측 상실·취소 경로를 확인하고, Classical 전환 전에 실제 팔/물체 상태를 대조한다.
- 동일 배치·재시도/시간 예산에서 성공률·전체 시간·사람 개입·낙하·데이터/학습 비용을 비교한다. Classical 복구로 완료한 시도는 별도 집계한다.

**G4 완료:** 재현 가능한 데이터/학습/배포 리비전과 비교 결과. 성능이 낮으면 그 결과와 실패 범위를 남기며, 검증되지 않은 정책을 기본 배포로 바꾸지 않는다.

## 5. 문서 운영과 다음 작업

- 이 문서는 우선순위·의존성·통과 기준의 단일 목록이다. [CURRENT_STATUS](CURRENT_STATUS.md)는 실제 구현/시험 결과, [ARCHITECTURE](ARCHITECTURE.md)는 통신/상태/단위/제한 계약, [hardware/README](../hardware/README.md)는 장비/실측 근거를 유지한다. 날짜별 개발 메인 문서는 추가하지 않는다.
- 상세 bag/영상/시험표/측정 기록은 코드·보정·모델/지도 ID를 붙인 산출물로 남긴다. H0에서 허용치·횟수·하중·시간의 초안을 잡고 해당 단계 평가 전에 고정한다.
- 지금 할 일은 갱신된 문서를 포함한 커밋 준비까지다. 실제 장비를 연결할 때 **H0부터 재개**한다. 부품 대기 중 추가 하드웨어 없는 개발은 요구 변경이나 재현된 결함이 있을 때만 수행한다.

## 2026-09-16 추가 점검 결과와 범위

- 완료한 PC 보완: Mini 1 공식 리비전/구성 확인, stamped SI 주행 브릿지와 실측 sample odom, 공통 정지·주행 owner 연결, native depth worker/점유 projection/관측 기반 통과 검사, 지도 교체의 비동기 정지 경계, 앞뒤 RGB + lift depth/colour 후보 TF, 카메라 화각 검사. 자세한 계약은 ARCHITECTURE의 재점검 절에 있다.
- **H0의 선행 판단을 강화:** 조립 후 카메라 강성·바퀴 축 유격·리프트 케이블/하중·전신/근거리 시야·전원 분배를 확인한다. H4 마지막에 발견해서 구조를 다시 바꾸지 않는다. PID는 이번에 변경하지 않았다.
- 단일 D415가 전신 정지 거리를 충분히 볼 수 있다는 결론은 아직 없다. 소프트웨어 후보는 관측이 없으면 차단하며, 기구/보완 센서가 필요한 경우 그대로 남긴다. 실험 전에 임의 physical 설정·재료별 성능·VLM 정답을 만들어 넣지 않는다.
- 이번 후보는 Python 775개·CTest 17개, STM32 2개 빌드, ROS 4개 패키지와 실제 Nav2 costmap 계층/DDS/C/MoveIt를 포함해 검증한다. 기록은 `output/navigation-audit-20260916/report.json`으로 분리한다. 과거 Nav2 전체 알고리즘 기록과 새 DDS/C 연결 기록을 혼합 집계하지 않는다.

### 일반화 목표에 대한 완료 조건 보강

현재 G1–G4는 대표 배달/언어 요청/폰 이동/정지 팔 정책 비교의 기반이다. 그것만으로
“모르는 집의 모든 빨래를 찾아 미지의 빨래통에 넣는다”까지 달성했다고 하지 않는다.
그 목표를 선택하면 G1/E1 이후 **의미 장소/물체 메모리 → 미지 목표 탐색 → 전체 수거 종료 판정**을
추가한다. 앞뒤 RGB로 후보를 찾고 접근·정지·리프트 D415로 3D 위치를 확인한다.
등록된 유한 시점 재탐색과 미지 집 전체 탐색을 구분한다.

평가는 접근 가능·노출된 대상과 공간 범위를 먼저 정의하고, 가구/빨래통 이동, 새로운 외형,
가려짐·미발견, 반복 수거/중복 방지, 빨래통 가득 참/집기 실패와 재시도, 정해진 탐색 영역의
미관측 부분 보고를 포함해야 한다. “탐지 결과가 0개”는 “빨래가 전부 수거됨”의 증거가 아니다.
실제 모델/데이터·목표 장면이 필요한 이 확장을 이미 구현된 기능처럼 부르지 않는다. 첫 배달
기준선을 완료하면서 관측/행동 로그를 저장해 후속 일반화 비교에 재사용한다.
